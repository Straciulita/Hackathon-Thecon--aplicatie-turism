import { View } from 'react-native';

// Acest fișier este gol intenționat. 
// El există doar ca să apară butonul în meniu.
export default function ToggleThemePlaceholder() {
  return <View style={{ flex: 1, backgroundColor: 'transparent' }} />;
}